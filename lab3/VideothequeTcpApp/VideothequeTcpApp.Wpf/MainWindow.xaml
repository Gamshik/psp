<Window x:Class="VideothequeTcpApp.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:VideothequeTcpApp.Wpf"
        xmlns:sys="clr-namespace:System;assembly=System"
        Title="MainWindow" Height="600" Width="1000">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TabControl>
            <TabItem Header="Videos">
                <DataGrid x:Name="VideosDataGrid" AutoGenerateColumns="False" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="200"/>
                        <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                        <DataGridTextColumn Header="Genre" Binding="{Binding Genre}" Width="100"/>
                        <DataGridTextColumn Header="Year" Binding="{Binding Year}" Width="70"/>
                        <DataGridTextColumn Header="Director" Binding="{Binding Director}" Width="150"/>
                        <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="70"/>
                        <DataGridTextColumn Header="Available Copies" Binding="{Binding AvailableCopies}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Customers">
                <DataGrid x:Name="CustomersDataGrid" AutoGenerateColumns="False" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="200"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="150"/>
                        <DataGridTextColumn Header="Phone" Binding="{Binding PhoneNumber}" Width="120"/>
                        <DataGridTextColumn Header="Registered At" Binding="{Binding RegisteredAt}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Rentals">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Label Content="Select Customer:" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <ComboBox x:Name="CustomerComboBox" Width="200" DisplayMemberPath="Name" SelectedValuePath="Id"/>
                        <Label Content="Select Video:" Margin="10,0,10,0" VerticalAlignment="Center"/>
                        <ComboBox x:Name="VideoComboBox" Width="200" DisplayMemberPath="Title" SelectedValuePath="Id" />
                    </StackPanel>
                    <DataGrid x:Name="RentalsDataGrid" AutoGenerateColumns="False" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="200"/>
                            <DataGridTextColumn Header="Customer ID" Binding="{Binding CustomerId}" Width="150"/>
                            <DataGridTextColumn Header="Video ID" Binding="{Binding VideoId}" Width="150"/>
                            <DataGridTextColumn Header="Start Date" Binding="{Binding StartDate}" Width="120"/>
                            <DataGridTextColumn Header="End Date" Binding="{Binding EndDate}" Width="120"/>
                            <DataGridTextColumn Header="Returned" Binding="{Binding IsReturned}" Width="80"/>
                            <DataGridTextColumn Header="Total Price" Binding="{Binding TotalPrice}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </TabItem>
            <TabItem Header="Reports">
                <StackPanel>
                    <DatePicker x:Name="ReportFromDate" SelectedDate="2024-10-01" Margin="0,0,10,10"/>
                    <DatePicker x:Name="ReportToDate" SelectedDate="2025-10-11" Margin="0,0,10,10"/>
                    <Button Content="Generate Report" Click="GenerateReportButton_Click" Width="120"/>
                    <TextBox x:Name="ReportTextBox" Height="200" TextWrapping="Wrap" Margin="0,10,0,0" IsReadOnly="True"/>
                </StackPanel>
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Load" Width="80" Click="LoadButton_Click" Margin="0,0,10,0"/>
            <Button Content="Add" Width="80" Click="AddButton_Click" Margin="0,0,10,0"/>
            <Button Content="Update" Width="80" Click="UpdateButton_Click" Margin="0,0,10,0"/>
            <Button Content="Delete" Width="80" Click="DeleteButton_Click"/>
        </StackPanel>
    </Grid>
</Window>