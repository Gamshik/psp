{
  "description": "Message protocol for BrainRing (edit before generating types)",
  "types": {
    "join": {
      "payload": { "name": "string", "roomId": "string" }
    },
    "question": {
      "payload": { "questionText": "string", "questionId": "string" }
    },
    "answer": {
      "payload": { "answer": "string", "questionId": "string" }
    },
    "score_update": {
      "payload": {
        "scores": { "patternProperties": { ".*": { "type": "number" } } }
      }
    }
  }
}
